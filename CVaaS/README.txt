# Computer Vision Semester Project

## Instructions to run frontend

1. Run cmd inside Client directory.
2. Type command `http-server` and press enter.

## Instructions to run backend

1. Run cmd inside CVaaS directory.
2. Activate environment by entering `Scripts\activate` and press enter.
3. Run command `cd Server` and press enter.
4. Run backend using command `python app.py`.
